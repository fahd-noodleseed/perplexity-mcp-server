# Environment variables for noodle-perplexity-mcp

# Logging level (debug, info, warn, error)
# Optional: Defaults to "info"
LOG_LEVEL=info

# Node environment (development, production)
# Optional: Defaults to "development"
NODE_ENV=development

# Perplexity API Key
# Required: Get your key from https://docs.perplexity.ai/docs/getting-started
PERPLEXITY_API_KEY=YOUR_PERPLEXITY_API_KEY_HERE

# MCP Transport settings
# Optional: Defaults to "stdio"
MCP_TRANSPORT_TYPE=stdio

# HTTP server settings (only used if MCP_TRANSPORT_TYPE=http)
# Optional: Defaults to 3010
MCP_HTTP_PORT=3010

# Optional: Defaults to "127.0.0.1"
MCP_HTTP_HOST=127.0.0.1

# Optional: Defaults to "info"
MCP_LOG_LEVEL=info

# HTTP authentication settings (only used if MCP_TRANSPORT_TYPE=http)
# Optional: Defaults to "jwt"
MCP_AUTH_MODE=jwt

# Required for JWT auth when using HTTP transport
MCP_AUTH_SECRET_KEY=your-secret-key-here-min-32-chars
